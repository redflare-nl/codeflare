node_modules/
dist/
*.vsix

# CodeFlare runtime records (metrics, experiments, runs, lab, checkpoints, memory)
.codeflare/
.codeflare-trash/

# Per-machine Claude Code settings — machine-specific (local endpoints, model
# names, absolute paths to other projects) and not meant for a public repo.
.claude/settings.json
.claude/settings.local.json

# Editor/OS noise
.vscode-test/
Thumbs.db
.DS_Store
